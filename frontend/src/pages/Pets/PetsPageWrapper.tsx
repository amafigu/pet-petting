import { styled } from '@mui/material/styles'

export const PetsPageWrapper = styled('main')(() => ({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'flex-start',
  alignItems: 'center',
  minHeight: '90vh',
  background: `linear-gradient(to right, #F17AC2, #713770);`
}))
